<%= form_for(location_list) do |f| %>
  <% if location_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location_list.errors.count, "error") %> prohibited this location_list from being saved:</h2>

      <ul>
      <% location_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :profile_id %>
    <%= f.text_field :profile_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
